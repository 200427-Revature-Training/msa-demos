# It's important for the application to load remote configuration early on # in its configuration lifecycle.  Configuration supplied in application.yaml is# loaded too late for this purpose.  bootstrap.yml will load early on in the app# lifecycle, allowing the app to configure itself in time to load config details # from the config servicespring:  application:    name: gateway-service
